import React, { useState, useEffect } from 'react';
import { StyleSheet, View, Text, ScrollView, TouchableOpacity, Dimensions, Image, Alert } from 'react-native';
import { FontAwesome5, MaterialIcons, Entypo } from '@expo/vector-icons';

// --- CONFIGURATION DES RÈGLES RPP ---
const RPP_RULES = {
  REWARD_15K_VIEWS: 1.00,
  REWARD_900_LIKES: 1.00,
  REWARD_750_COMMENTS: 1.00,
  REWARD_550_FAVORITES: 1.00,
  REWARD_10_SUBS: 1.00,
  RPP_COMMISSION: 0.20, // 20% que l'utilisateur ne voit pas
  MIN_DURATION: 61, // 1 min 1 sec
  MIN_FOLLOWERS_MONETIZE: 1500,
  MIN_AGE: 18,
};

export default function RPPApp() {
  const [isVpnDetected, setIsVpnDetected] = useState(false);
  const [activeTab, setActiveTab] = useState('Home');
  const [userBalance, setUserBalance] = useState(0.00); // Visible par l'utilisateur (déjà déduit de 20%)

  // 1. DÉTECTION VPN (SIMULATION)
  useEffect(() => {
    checkVPN();
  }, []);

  const checkVPN = async () => {
    // Logique pour vérifier si l'IP appartient à un fournisseur VPN
    // Si détecté : setIsVpnDetected(true);
  };

  if (isVpnDetected) {
    return (
      <View style={[styles.container, { justifyContent: 'center' }]}>
        <Text style={styles.errorText}>L'accès à RPP est interdit via un VPN.</Text>
      </View>
    );
  }

  // 2. LOGIQUE DE CALCUL DE RÉCOMPENSE (CACHÉE)
  const calculateReward = (totalEarned) => {
    const rppTake = totalEarned * RPP_RULES.RPP_COMMISSION;
    const userTake = totalEarned - rppTake;
    return userTake; // On ne montre que ça à l'utilisateur
  };

  return (
    <View style={styles.container}>
      {/* --- AFFICHAGE SELON L'ONGLET --- */}
      {activeTab === 'Home' && <HomeFeed />}
      {activeTab === 'Shop' && <ShopBusiness />}
      {activeTab === 'Wallet' && <WalletSystem balance={userBalance} />}
      {activeTab === 'Profile' && <ProfileScreen />}

      {/* --- BARRE DE NAVIGATION (STYLE TIKTOK) --- */}
      <View style={styles.navBar}>
        <NavButton icon="home" label="Accueil" active={activeTab === 'Home'} onPress={() => setActiveTab('Home')} />
        <NavButton icon="shopping-bag" label="Shop" active={activeTab === 'Shop'} onPress={() => setActiveTab('Shop')} />
        <TouchableOpacity style={styles.plusButton}><Entypo name="plus" size={30} color="black" /></TouchableOpacity>
        <NavButton icon="wallet" label="Bourse" active={activeTab === 'Wallet'} onPress={() => setActiveTab('Wallet')} />
        <NavButton icon="user" label="Profil" active={activeTab === 'Profile'} onPress={() => setActiveTab('Profile')} />
      </View>
    </View>
  );
}

// --- COMPOSANT ACCUEIL (FLUX VIDÉO) ---
const HomeFeed = () => (
  <View style={styles.fullScreen}>
    <Image source={{ uri: 'via.placeholder.com' }} style={styles.videoPlaceholder} />
    <View style={styles.sideButtons}>
      <ActionButton icon="heart" label="900" />
      <ActionButton icon="comment" label="750" />
      <ActionButton icon="star" label="550" />
      <ActionButton icon="share" label="Pataje" />
      <TouchableOpacity style={styles.btnAnalyse} onPress={() => Alert.alert("Analyse", "Seul vous pouvez voir ceci : Revni: $1.00")}>
        <MaterialIcons name="analytics" size={24} color="white" />
      </TouchableOpacity>
    </View>
  </View>
);

// --- COMPOSANT SHOP & ENTREPRISE ---
const ShopBusiness = () => (
  <View style={styles.pageContent}>
    <View style={styles.tabHeader}>
      <Text style={styles.tabTitleActive}>ENTREPRISE</Text>
      <Text style={styles.tabTitle}>SHOP</Text>
    </View>
    <ScrollView>
      <Text style={styles.whiteText}>Konbyen kòb ou vann: $0.00</Text>
      <Text style={styles.whiteText}>Konbyen kòb ou achte: $0.00</Text>
    </ScrollView>
  </View>
);

// --- COMPOSANT WALLET (RETRAIT/DÉPÔT) ---
const WalletSystem = ({ balance }) => (
  <View style={styles.pageContent}>
    <Text style={styles.balanceLabel}>Balanse ou (Chak Lendi)</Text>
    <Text style={styles.balanceAmount}>{balance} USD</Text>
    <View style={styles.paymentGrid}>
      {['MonCash', 'NatCash', 'PayPal', 'Zelle', 'Visa', 'MasterCard'].map(method => (
        <TouchableOpacity key={method} style={styles.methodBtn}><Text style={styles.methodText}>{method}</Text></TouchableOpacity>
      ))}
    </View>
  </View>
);

// --- COMPOSANTS DE SOUTIEN ---
const NavButton = ({ icon, label, active, onPress }) => (
  <TouchableOpacity onPress={onPress} style={styles.navItem}>
    <FontAwesome5 name={icon} size={20} color={active ? "#FF0000" : "#FFF"} />
    <Text style={{ color: active ? "#FF0000" : "#FFF", fontSize: 10 }}>{label}</Text>
  </TouchableOpacity>
);

const ActionButton = ({ icon, label }) => (
  <View style={styles.actionItem}>
    <FontAwesome5 name={icon} size={30} color="white" />
    <Text style={styles.actionLabel}>{label}</Text>
  </View>
);

// --- STYLES ---
const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#000' },
  fullScreen: { flex: 1 },
  pageContent: { flex: 1, paddingTop: 60, paddingHorizontal: 20 },
  videoPlaceholder: { width: '100%', height: '100%' },
  whiteText: { color: '#FFF', marginVertical: 10 },
  errorText: { color: 'red', textAlign: 'center', fontWeight: 'bold' },
  navBar: { flexDirection: 'row', height: 70, borderTopWidth: 0.5, borderColor: '#333', backgroundColor: '#000', alignItems: 'center' },
  navItem: { flex: 1, alignItems: 'center' },
  plusButton: { width: 45, height: 35, backgroundColor: '#FFF', borderRadius: 10, justifyContent: 'center', alignItems: 'center' },
  sideButtons: { position: 'absolute', right: 10, bottom: 100, alignItems: 'center' },
  actionItem: { marginBottom: 20, alignItems: 'center' },
  actionLabel: { color: 'white', fontSize: 12 },
  btnAnalyse: { marginTop: 10, backgroundColor: 'rgba(255,0,0,0.5)', padding: 8, borderRadius: 20 },
  balanceLabel: { color: '#AAA', fontSize: 16, textAlign: 'center' },
  balanceAmount: { color: '#FFF', fontSize: 40, fontWeight: 'bold', textAlign: 'center', marginVertical: 20 },
  paymentGrid: { flexDirection: 'row', flexWrap: 'wrap', justifyContent: 'center' },
  methodBtn: { width: '40%', backgroundColor: '#1A1A1A', padding: 15, margin: 10, borderRadius: 10, borderWidth: 1, borderColor: '#FF0000' },
  methodText: { color: '#FFF', textAlign: 'center', fontWeight: 'bold' },
  tabHeader: { flexDirection: 'row', justifyContent: 'space-around', marginBottom: 20 },
  tabTitleActive: { color: '#FF0000', fontWeight: 'bold', borderBottomWidth: 2, borderColor: '#FF0000' },
  tabTitle: { color: '#FFF' }
});
